<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Virtual Holiday Gift Exchange</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Mountains+of+Christmas:wght@700&display=swap');
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: #0d3b66;
            background-image: url("data:image/svg+xml,%3Csvg width='52' height='26' viewBox='0 0 52 26' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.04'%3E%3Cpath d='M10 10c0-2.21-1.79-4-4-4-3.314 0-6-2.686-6-6h2c0 2.21 1.79 4 4 4 3.314 0 6 2.686 6 6 0 2.21 1.79 4 4 4 3.314 0 6 2.686 6 6 0 2.21 1.79 4 4 4v2c-3.314 0-6-2.686-6-6 0-2.21-1.79-4-4-4-3.314 0-6-2.686-6-6zm25.464-1.95l8.486 8.486-1.414 1.414-8.486-8.486 1.414-1.414z' /%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
            color: #fff;
            text-align: center;
            padding: 2rem;
        }
        h1 {
            font-family: 'Mountains of Christmas', cursive;
            font-size: 4.5rem;
            color: #f7b267;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
            margin: 0 0 2rem 0;
        }
        #instructions {
            font-size: 1.2rem;
            max-width: 800px;
            margin: 0 auto 2rem auto;
            color: #faf0ca;
        }
        #gift-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 1.5rem;
            max-width: 1000px;
            margin: 0 auto;
        }
        .box {
            aspect-ratio: 1 / 1;
            background: #d90429;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
            font-weight: 700;
            color: #f4d35e;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            box-shadow: 0 5px 15px rgba(0,0,0,0.3);
            overflow: hidden;
        }
        /* CSS Ribbon */
        .box::before {
            content: '';
            position: absolute;
            width: 30%;
            height: 100%;
            background: #f7b267;
        }
        .box::after {
            content: '';
            position: absolute;
            width: 100%;
            height: 30%;
            background: #f7b267;
        }
        .box span {
            z-index: 1;
            background: rgba(0,0,0,0.3);
            border-radius: 50%;
            width: 60px;
            height: 60px;
            line-height: 60px;
        }
        .box:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 8px 20px rgba(0,0,0,0.4);
        }
        .box.opened {
            background: #555;
            color: #888;
            cursor: not-allowed;
            opacity: 0.6;
            transform: scale(0.95);
        }
        .box.opened::before, .box.opened::after {
            background: #777;
        }

        /* Modal */
        .modal {
            display: none; 
            position: fixed; 
            z-index: 100; 
            left: 0; top: 0;
            width: 100%; height: 100%; 
            overflow: auto; 
            background-color: rgba(0,0,0,0.7);
        }
        .modal-content {
            background-color: #fefefe;
            color: #333;
            margin: 10% auto;
            padding: 30px;
            border: 1px solid #888;
            width: 80%;
            max-width: 500px;
            border-radius: 8px;
            text-align: center;
            position: relative;
        }
        .close-button {
            color: #aaa;
            position: absolute;
            top: 10px;
            right: 20px;
            font-size: 28px;
            font-weight: bold;
            cursor: pointer;
        }
        .close-button:hover, .close-button:focus { color: black; }
        #modal-img {
            width: 100%;
            max-height: 400px;
            object-fit: contain;
            border-radius: 4px;
            margin-bottom: 1rem;
        }
        #modal-gift-name { font-size: 2rem; margin: 0; }
        #modal-from { font-size: 1.2rem; color: #555; margin: 0.5rem 0 1rem 0; }
        #modal-link a {
            display: inline-block;
            padding: 0.75rem 1.5rem;
            background: #2a9d8f;
            color: white;
            text-decoration: none;
            border-radius: 4px;
            font-weight: 700;
        }
    </style>
</head>
<body>

    <h1>Virtual Gift Exchange</h1>
    <p id="instructions">Welcome! When it's your turn, choose a present box to reveal the gift!</p>
    <div id="gift-grid"></div>

    <div id="gift-modal" class="modal">
        <div class="modal-content">
            <span class_id="close-button" id="modal-close">&times;</span>
            <img id="modal-img" src="" alt="Gift Image">
            <h2 id="modal-gift-name">Gift Name</h2>
            <p id="modal-from">Brought by: Name</p>
            <p id="modal-link"><a href="#" target="_blank">View Gift</a></p>
        </div>
    </div>

    <script>
        // --- Game Setup ---
        const allParticipants = [
            "Ron Mantel", "Kendra Bernstein", "Swathi Krothapalli", "Sabrina Dobbs", "Omar Garcia", 
            "Sean Stallings", "Esther Ly", "Kate Elisha", "Ebony Dixon", "Jillian DiPaolo"
        ];

        // Utility: Fisher-Yates Shuffle
        function shuffle(array) {
            let currentIndex = array.length, randomIndex;
            while (currentIndex != 0) {
                randomIndex = Math.floor(Math.random() * currentIndex);
                currentIndex--;
                [array[currentIndex], array[randomIndex]] = [array[randomIndex], array[currentIndex]];
            }
            return array;
        }

        // --- Modal Logic ---
        const modal = document.getElementById("gift-modal");
        const modalImg = document.getElementById("modal-img");
        const modalGiftName = document.getElementById("modal-gift-name");
        const modalFrom = document.getElementById("modal-from");
        const modalLink = document.getElementById("modal-link").firstElementChild;
        const closeModal = document.getElementById("modal-close");

        closeModal.onclick = () => modal.style.display = "none";
        window.onclick = (event) => {
            if (event.target == modal) {
                modal.style.display = "none";
            }
        }

        // --- Main Game Logic ---
        window.addEventListener('load', function() {
            const gifts = JSON.parse(localStorage.getItem('secretSantaGifts')) || [];
            
            if (gifts.length < allParticipants.length) {
                document.getElementById('instructions').innerHTML = 
                    `<strong style="color: #f00;">WAIT! Not all gifts have been submitted.</strong><br>
                    Expected ${allParticipants.length} gifts, but only found ${gifts.length}.<br>
                    Please check your 'admin.html' page.`;
                return;
            }

            const shuffledGifts = shuffle(gifts);
            const grid = document.getElementById('gift-grid');

            shuffledGifts.forEach((gift, index) => {
                const box = document.createElement('div');
                box.classList.add('box');
                box.innerHTML = `<span>${index + 1}</span>`;
                box.dataset.giftIndex = index; // Store the index of the gift

                box.addEventListener('click', openBox);
                grid.appendChild(box);
            });

            function openBox(e) {
                const box = e.currentTarget;
                if (box.classList.contains('opened')) {
                    alert('This gift has already been opened!');
                    return;
                }
                
                // Mark as opened
                box.classList.add('opened');

                // Get gift data
                const giftIndex = box.dataset.giftIndex;
                const gift = shuffledGifts[giftIndex];

                // Populate and show modal
                modalImg.src = gift.img;
                modalGiftName.textContent = gift.giftName;
                modalFrom.textContent = `Brought by: ${gift.from}`;
                modalLink.href = gift.link;
                
                modal.style.display = "block";
            }
        });
    </script>
</body>
</html>
